Please implement a navigation bar component with the following specifications:

1. Architectural Layout & State:

    The navbar must be horizontally centered at the top of the viewport (fixed position, z-index 50).

    Implement a state-driven behavior:

        Expanded State: When the user is at the top of the page (Hero section), the navbar is an expanded, rounded rectangle with padding, displaying the logo and navigation links.

        Collapsed State: When the user scrolls down, the navbar must smoothly transition into a small, elegant rhombus shape (diamond-like orientation).

    Interaction: When the user hovers over the collapsed rhombus, it must spring back to the expanded state.

2. Kinetic UI/UX & Visuals:

    Glassmorphism: Use a backdrop-filter (blur) with a semi-transparent dark background for the navbar container to give it a premium, floating aesthetic.

    Neon Detail: At the bottom center of the expanded navbar, include a 2px high 'neon blue' horizontal line. This line should have a gradient fade effect, appearing opaque in the center and fading to transparent at the ends.

    Transitions: Use smooth spring-based physics for the expansion/contraction animations (if using Framer Motion) or optimized CSS transitions to ensure the movement feels weighted and precise, not jittery.

3. Content:

    The expanded state should contain a logo placeholder on the left, primary navigation links (Capabilities, Protocol, Valuation) in the center, and a high-contrast 'Initialize' button on the right.

    The collapsed state should only display a single, centered downward-pointing arrow (or a geometric icon) that rotates to match the rhombus aesthetic.

Technical Constraints:

    Use Tailwind CSS for all styling (backdrop-blur, rounded-full, absolute/fixed positioning).

    Ensure the transition between states is fluid and does not cause layout shifts on the rest of the page.

    The design should feel like a piece of high-end aerospace or engineering equipment."